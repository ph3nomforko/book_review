class Category {
    constructor(category) {
        this.id = category.id
        this.name = category.attributes.name
        this.books = category.attributes.books
    }

    renderCategoryList() {
        
    }
}